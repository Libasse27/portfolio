# URL absolue du site, sans slash final. Utilisée par le sitemap, robots.txt,
# l'URL canonique et les métadonnées Open Graph.
#
# OBLIGATOIRE en production : le nom de domaine n'est pas encore acquis
# (voir docs/01-Business/PersonalBrand.md). Ne JAMAIS déployer avec la
# valeur par défaut (http://localhost:3000) — configurer la vraie variable
# d'environnement sur Vercel une fois le domaine réservé.
SITE_URL=http://localhost:3000
