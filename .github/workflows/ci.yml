name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  quality:
    name: Lint · Types · Tests · Build · Lighthouse
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Pas de `version:` ici : pnpm/action-setup@v4 lit `packageManager`
      # dans package.json (pnpm@9.15.0) — fixer les deux provoque
      # ERR_PNPM_BAD_PM_VERSION (versions en conflit).
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Installer les dépendances
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Vérification des types
        run: pnpm type-check

      - name: Tests unitaires
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Lighthouse CI
        run: pnpm --filter @portfolio/web exec lhci autorun
